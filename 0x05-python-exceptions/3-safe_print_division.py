#!/usr/bin/python3
def safe_print_division(a, b):
    a/b = result
    try: 
        if a%b = 0
        except: 
             
             finally: 
                Inside result: print("{}".format(result))
